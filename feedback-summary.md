# Feedback Summary

**Question:** What are the advantages of using upper bounded and lower bounded wildcards in Java, like `<? super Animal>` and `<? extends Animal>`?

**Feedback:** The candidate vaguely mentioned that bounded wildcards help with inheritance and support polymorphism, but the explanation was largely inaccurate. They incorrectly suggested that wildcards allow a class to inherit all attributes or use parent constructors, which does not describe the purpose of upper- and lower-bounded wildcards.
